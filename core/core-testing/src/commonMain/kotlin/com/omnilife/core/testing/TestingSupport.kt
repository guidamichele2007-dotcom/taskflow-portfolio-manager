package com.omnilife.core.testing

/**
 * Utility di test condivise (fixture, builder di entità di prova) per le suite unit/integration di tutti i moduli.
 *
 * Engineering Plan §05 (Testing Strategy).
 *
 * Segnaposto di modulo feature (L1/L2): nessuna schermata, nessun
 * componente UI in questo bootstrap. Ospiterà lo state holder MVI
 * (Technical Architecture Bible TDR-02) quando la Feature entrerà in
 * sviluppo.
 */
public object TestingSupport
